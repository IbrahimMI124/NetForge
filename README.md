# NetForge
A software IPv4 router in C++ from first principles using raw sockets, libpcap, and Linux network namespaces. Implemented packet parsing, longest-prefix route lookup, TTL handling, checksum recomputation, and bidirectional packet forwarding across virtual networks.
